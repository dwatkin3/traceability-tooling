
R2 Range Expansion — Developer Checklist
=======================================

Decisions Locked: R2 + S1 + F1 + E3 + A + D2 + Q20

[ ] range_expander.py implemented with:
    [ ] Unicode dash normalization
    [ ] Token splitting on ',', '&', 'AND'
    [ ] Numeric range parsing (prefix + zero-padded numeric)
    [ ] Family expansion via exec_families
    [ ] 'ALL' expansion via union of all exec families
    [ ] Deduplicate + sorted output (prefix, number)

[ ] plan_parser.py updated to:
    [ ] Preserve original spans (for Summary)
    [ ] Call expand_plan_items(raw_items, exec_ids, exec_families)
    [ ] Produce story_to_expanded_tests

[ ] audit_writer.py updated to:
    [ ] Use expanded sets for missing/covered/extra/status
    [ ] Keep Summary planned_list showing original spans
    [ ] Write Expanded_Plan sheet (Story, ExpandedTest)

[ ] reconcile logic uses expanded sets (no other changes needed)

[ ] run_release builds execution universe:
    [ ] exec_ids (Set[str])
    [ ] exec_families (Dict[str, List[str]]) grouped by prefix

[ ] Tests executed:
    [ ] Numeric ranges expansion (forward & reverse)
    [ ] Family tokens expansion
    [ ] Unions (comma, &, AND)
    [ ] ALL expansion takes all exec families
    [ ] Summary spans intact; status uses expanded
    [ ] GREEN/AMBER/RED examples verified

[ ] Outputs verified:
    [ ] outputs/<release>/Traceability_Reconciliation_<release>.xlsx
    [ ] Expanded_Plan tab present and populated
    [ ] debug/summary.csv correct

Notes:
- If a span expands to zero IDs (not present in exec this release), it contributes nothing this month.
- RED overrides AMBER. Per-story extras only.
- Keep Summary readable: spans only (E3).
